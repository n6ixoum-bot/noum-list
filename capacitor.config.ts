import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.noumlist.app",
  appName: "Noum List",
  webDir: "dist",
  android: {
    backgroundColor: "#080b09",
  },
};

export default config;
